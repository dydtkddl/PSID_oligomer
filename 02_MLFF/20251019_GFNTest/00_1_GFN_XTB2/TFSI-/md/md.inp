$md
  temp=300       # 온도 (K)
  time=50        # 총 MD 시간(ps)
  step=2.0       # 타임스텝(fs)
  dump=50        # 트래젝토리 저장 간격(fs)
  nvt=true       # Nose-Hoover thermostat
  shake=1        # X–H 결합 constrain
$end
